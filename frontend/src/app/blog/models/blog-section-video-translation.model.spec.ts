import { BlogSectionVideoTranslation } from './blog-section-video-translation.model';

describe('BlogSectionVideoTranslation', () => {
  it('should create an instance', () => {
    expect(new BlogSectionVideoTranslation()).toBeTruthy();
  });
});
