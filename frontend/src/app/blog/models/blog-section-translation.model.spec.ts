import { BlogSectionTranslation } from './blog-section-translation.model';

describe('BlogSectionTranslation', () => {
  it('should create an instance', () => {
    expect(new BlogSectionTranslation()).toBeTruthy();
  });
});
