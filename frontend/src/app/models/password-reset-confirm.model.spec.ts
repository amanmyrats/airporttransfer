import { PasswordResetConfirm } from './password-reset-confirm.model';

describe('PasswordResetConfirm', () => {
  it('should create an instance', () => {
    expect(new PasswordResetConfirm()).toBeTruthy();
  });
});
