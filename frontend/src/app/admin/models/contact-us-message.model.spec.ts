import { ContactUsMessage } from './contact-us-message.model';

describe('ContactUsMessage', () => {
  it('should create an instance', () => {
    expect(new ContactUsMessage()).toBeTruthy();
  });
});
