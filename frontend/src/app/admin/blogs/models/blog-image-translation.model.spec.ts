import { BlogImageTranslation } from './blog-image-translation.model';

describe('BlogImageTranslation', () => {
  it('should create an instance', () => {
    expect(new BlogImageTranslation()).toBeTruthy();
  });
});
