import { BlogVideoTranslation } from './blog-video-translation.model';

describe('BlogVideoTranslation', () => {
  it('should create an instance', () => {
    expect(new BlogVideoTranslation()).toBeTruthy();
  });
});
