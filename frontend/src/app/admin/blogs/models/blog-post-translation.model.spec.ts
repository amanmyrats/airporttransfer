import { BlogPostTranslation } from './blog-post-translation.model';

describe('BlogPostTranslation', () => {
  it('should create an instance', () => {
    expect(new BlogPostTranslation()).toBeTruthy();
  });
});
