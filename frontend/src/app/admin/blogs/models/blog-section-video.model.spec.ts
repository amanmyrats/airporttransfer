import { BlogSectionVideo } from './blog-section-video.model';

describe('BlogSectionVideo', () => {
  it('should create an instance', () => {
    expect(new BlogSectionVideo()).toBeTruthy();
  });
});
