import { LocalizedBlogPost } from './localized-blog-post.model';

describe('LocalizedBlogPost', () => {
  it('should create an instance', () => {
    expect(new LocalizedBlogPost()).toBeTruthy();
  });
});
