import { RoleNameDirective } from './role-name.directive';

describe('RoleNameDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleNameDirective();
    expect(directive).toBeTruthy();
  });
});
