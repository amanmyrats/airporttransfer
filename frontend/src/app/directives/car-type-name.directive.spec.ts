import { CarTypeNameDirective } from './car-type-name.directive';

describe('CarTypeNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CarTypeNameDirective();
    expect(directive).toBeTruthy();
  });
});
